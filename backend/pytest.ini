[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Run `async def` tests without a per-test marker; pytest-asyncio provides
# the event loop. Needed for the aiogram throttling-middleware tests.
asyncio_mode = auto
