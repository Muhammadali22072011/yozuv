Confirmed: `my_bookings.py:70` shows the `rev:` review button on **active** bookings (PENDING/CONFIRMED, the `active` list from line 31-40), and `start_review` (line 215) only blocks CANCELLED. So a client can post a rating for a service they merely *booked* but haven't received yet.

I have enough verified, distinct findings. Let me compile the report.

Based on my review of the booking flow, backend services, bot handlers, and frontend data flow, here are NEW high-confidence findings not in the audit:

---

**`backend/bot/handlers/my_bookings.py:55-74, 205-223` — medium — correctness** — The booking-tied review path lets a client rate a visit that hasn't happened. `cmd_mybookings` renders the `rev:{id}` "Baho berish" button on the **active** list (PENDING/CONFIRMED bookings, lines 31-40 → 70), and `start_review`/`_save_review_from_state` only reject `CANCELLED` (lines 215-217, 296-297) — never requiring `COMPLETED` or `date <= today`. So a client can book a slot for next week and immediately post a 5-star (or 1-star) review that feeds the public average/sort in `start.py:325-342`. The audit's finding #20 explicitly called this booking-tied branch "properly guarded"; it is not. Fix: in `start_review` and the `bid` branch of `_save_review_from_state`, require `booking.status == COMPLETED` (and only render the `rev:` button on the `completed` list, not `active`).

**`backend/app/routers/bookings.py:353-396` (vs `my_bookings.py:356-375`, `owner.py:85-114`) — medium — correctness** — Waitlist notification only fires on the **dashboard** cancel path. The owner-API `cancel` calls `waitlist_service.notify_first_for_slot` (lines 369-388), but the two other cancel paths that free the exact same slot do not: the client self-cancel in the bot (`my_bookings.cxl` → `cancel_booking`, line 370) and the owner reject in the bot (`owner.rej:` → `booking_service.cancel_booking`, line 100). A client sitting at the head of the waitlist is silently never pinged when the slot is freed via Telegram — which is the most common cancel route. Fix: move the `notify_first_for_slot` hop into a shared helper invoked by all three cancel call sites (or into `cancel_booking` itself), not just the one router endpoint.

**`frontend/src/components/yz/NewBookingSheet.tsx:61, 254-259` — low — correctness** — The owner "new booking" date picker has no lower bound, so a past date is freely selectable (`<input type="date">` with no `min`), and `finish()` POSTs it. The backend `create_owner_booking` never rejects past dates either, so an owner can create bookings dated in the past, which then skew analytics and the loyalty COMPLETED-count logic. Fix: add `min={isoFor(new Date())}` to the date input (and ideally reject past `date` server-side in `create_owner_booking`).

**`backend/app/services/booking_service.py:189-205` — low — correctness** — The idempotent "same-slot rebook returns existing" guard ignores `staff_id`. It matches an existing PENDING/CONFIRMED booking for `(business, client, date, start_time)` with no `staff_id` filter (lines 195-201), then `return own_existing`. In a multi-staff business a client who books staff A at 14:00, then deliberately books staff B at 14:00, gets handed back the staff-A booking instead of a new staff-B booking — a silently wrong result (and the second master is never booked). Fix: add `Booking.staff_id == staff_id` to the `own_existing` query so idempotency is scoped per-staff, matching `_check_slot_available`.

**`backend/bot/handlers/booking.py:266-288` — low — correctness** — `pick_staff` trusts the staff UUID from `stfok:{sid}:{staff_id}` callback data and stashes it into FSM state with no validation that the staff belongs to this business or can perform this service. While `create_booking` later validates business+active ownership (so cross-tenant booking is blocked), it does **not** re-check the service↔staff mapping that `_staff_for_service` enforced at pick time. A crafted callback (or a staff member later un-mapped from the service) lets a booking be created against a staff who cannot do that service. Fix: validate `staff_id` against `_staff_for_service(db, b.id, service.id)` in `pick_staff` before storing it, and/or re-check the mapping in `create_booking`.

**`backend/app/services/booking_service.py:222-225` (also `routers/bookings.py:193-195`) — low — reuse** — The `ConfirmationMode` resolution block (the `try/except` that coerces `business.confirmation_mode` and falls back to `AUTO`, then maps to `CONFIRMED`/`PENDING`) is duplicated verbatim in three write paths: `create_booking`, `create_owner_booking` (bookings.py:186-195), and `create_recurring` (bookings.py:547-559). Drift risk: a change to the default/mapping must be made in three places. Fix: extract a single `resolve_initial_status(business) -> BookingStatus` helper in `booking_service` and call it from all three.

**`frontend/src/components/yz/NewBookingSheet.tsx:73-94` — low — efficiency** — Two effects refetch on every sheet interaction. The clients+services fetch effect depends on `[open, defaultDate, defaultTime, defaultClientId]` and the busy-slots effect on `[open, date]`. Because `setStep`/`setDate` inside the first effect run on each open and `date` changes as the user steps through, the clients and services lists (which never change during a booking) are re-fetched alongside legitimately-changing busy slots, and toggling `open` refetches everything. Minor, but the static client/service lists could be fetched once and cached. Fix: split the static-data fetch (clients/services) from the date-dependent busy-slots fetch so the former runs only on first open.

---

Note: I deliberately excluded items already in the audit (loyalty concurrency #47, late-cancel TZ #19, standalone reviews #20, recurring double-book #10, per-staff conflict gaps #11/#12, owner-confirm resurrection #4, cross-tenant client lookup #6). The two strongest new findings are the **review-before-visit gap** (`my_bookings.py`) and the **waitlist notification only on the dashboard cancel path** (`bookings.py` vs bot cancels).